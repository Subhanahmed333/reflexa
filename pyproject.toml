[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "reflexa"
version = "0.1.0"
description = "Autonomous bug-fixing MVP for the Reflexa PRD"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["Reflexa"]
py-modules = ["reflexa"]
